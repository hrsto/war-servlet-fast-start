<?xml version="1.0" encoding="UTF-8"?>
<jboss-web>
    <!-- In Wildfly the context root will precede every other path. If a Servlet's context path is /SERV, then the full address will be the-context-path/SERV. Context root is set here. If this line is omitted, then upon application upload to Wildfly deployments, you can set name and runtime name. They will default to the name of the .war archive... -->
    <context-root>/</context-root>
</jboss-web>